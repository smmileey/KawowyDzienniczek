<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"

    android:orientation="vertical"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/offersactivity_detail"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="pl.kawowydzienniczek.kawowydzienniczek.Activities.OffersDetailFragment">

    <!-- KawowyDzienniczekService progress -->
    <ProgressBar android:id="@+id/fragment_detail_product_progress"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginTop="8dp"
        android:visibility="gone"
        android:layout_gravity="center"/>

    <include layout="@layout/lw_promotions"
        android:id="@+id/promotion_detail_included_content">
    </include>

</LinearLayout>
